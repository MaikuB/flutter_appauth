{
  "name": "flutter_appauth_web_js",
  "version": "1.0.0",
  "private": true,
  "description": "Reproducible browser bundle of @openid/appauth (AppAuth-JS) for the flutter_appauth_web plugin. Produces ../assets/appauth.bundle.js exposing the library on window.AppAuth.",
  "license": "Apache-2.0",
  "scripts": {
    "build": "esbuild entry.js --bundle --format=iife --platform=browser --minify --sourcemap=external --outfile=../assets/appauth.bundle.js"
  },
  "dependencies": {
    "@openid/appauth": "1.3.2"
  },
  "devDependencies": {
    "esbuild": "0.25.0"
  },
  "allowScripts": {
    "esbuild@0.25.0": true
  }
}
